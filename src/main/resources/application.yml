spring:
  application:
    name: goa-webapp
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/goa_webapp}
    driver-class-name: org.postgresql.Driver
    username: ${SPRING_DATASOURCE_USERNAME:goa}
    password: ${SPRING_DATASOURCE_PASSWORD:goa}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: false
  sql:
    init:
      # Runs schema.sql against the real (non-embedded) Postgres database too, not just
      # H2/embedded DBs. schema.sql only contains idempotent, additive fixes for schema
      # changes Hibernate's ddl-auto:update can't do safely on its own (backfilling a new
      # NOT NULL column, dropping a constraint that no longer applies) - it's safe to run
      # on every startup and runs before Hibernate's own schema check.
      mode: always
      # schema.sql uses Postgres DO $$ ... $$ blocks, which contain their own semicolons;
      # Spring's script splitter doesn't understand $$ dollar-quoting and would otherwise
      # cut a block apart at its first internal ";". Terminating each top-level statement
      # with "//" instead tells it where statements actually end.
      separator: "//"
  servlet:
    multipart:
      max-file-size: ${APP_MAX_UPLOAD_SIZE:50MB}
      max-request-size: ${APP_MAX_UPLOAD_REQUEST_SIZE:200MB}
  thymeleaf:
    # Re-parsing every template on every request is fine for local dev (hot template edits)
    # but wastes CPU per request in production. Default to cached; set THYMELEAF_CACHE=false
    # locally if you need template edits to show up without a restart.
    cache: ${THYMELEAF_CACHE:true}
  mail:
    host: ${MAIL_HOST:}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:true}
          starttls:
            enable: ${MAIL_SMTP_STARTTLS:true}
          # Without these, JavaMail's defaults are *infinite* - an unreachable/misconfigured
          # MAIL_HOST then hangs the admin's send request forever instead of failing with a
          # visible error. 10s is generous for a normal SMTP handshake.
          connectiontimeout: 10000
          timeout: 10000
          writetimeout: 10000

server:
  port: 8080
  # Without this, ServletUriComponentsBuilder (used to build absolute URLs for
  # newsletter images/links) trusts only the raw request Tomcat sees - "http" and
  # whatever internal host/port a reverse proxy connects with - instead of the
  # X-Forwarded-* headers a proxy sets for the real public scheme/host. APP_BASE_URL
  # below still overrides both when set.
  forward-headers-strategy: framework

app:
  # The public URL recipients' mail clients need to reach for newsletter images/links
  # (e.g. https://goa-festival.de) - set this if the admin panel is reached at a
  # different address than that (an internal network, a different scheme). Left
  # blank, the current request's own URL is used, which is fine for a simple setup
  # with no reverse proxy in front.
  base-url: ${APP_BASE_URL:}
  upload-dir: uploads
  admin:
    username: ${ADMIN_USERNAME:admin}
    password: ${ADMIN_PASSWORD:admin}
  mail:
    # Sender address for outgoing newsletter mails - most SMTP providers reject
    # sending as an address the authenticated account doesn't own, so this
    # usually has to match (or be a verified alias of) MAIL_USERNAME. Falls back
    # to MAIL_USERNAME in NewsletterService if left blank (docker-compose always
    # sets this env var, even if empty, so a plain ":default" YAML placeholder
    # wouldn't kick in the way it would for a truly unset property).
    from: ${MAIL_FROM:}
  recaptcha:
    # Google reCAPTCHA v2 ("I'm not a robot" checkbox) on the newsletter signup form.
    # Leave both blank to skip the check entirely (e.g. local dev).
    site-key: ${RECAPTCHA_SITE_KEY:}
    secret-key: ${RECAPTCHA_SECRET_KEY:}

logging:
  level:
    de.goafestival.webapp: INFO
